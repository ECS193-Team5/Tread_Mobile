import { Platform, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	navBarStyle: {
		height :85 ,
		overflow : 'hidden',
		borderRadius : 40,
		position: 'absolute',
		borderColor : "#014421",
		borderWidth : 0.5,
		borderTopColor : "#014421",
		borderTopWidth :0.5
	}
});

export {styles}